// You can initialize the array by accessing the `index` variable which
// gives the index as shown here: 

count := []int{len: 4, init: index}
assert count == [0, 1, 2, 3]

mut square := []int{len: 6, init: index * index }

println("square: ${square}")

